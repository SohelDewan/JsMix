const numbers = [12, 98, 5, 41, 23, 78, 46];

for(let i=0; i<numbers.length; i++){
    if(numbers[i]%2==0){
        let new_nums = numbers[i];
        // console.log(new_nums);
    }
    // new_numbers.push(numbers[i])
    // console.log(new_numbers);
}
let new_numbers = [];
new_numbers.push(new_nums);